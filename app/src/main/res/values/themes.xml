<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Passlocker" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- base colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/colorPrimary</item>
        <item name="android:background">@color/colorPrimary</item>

        <!-- system bar color. -->
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@color/colorPrimary</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>

        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textSecondary</item>
        <item name="android:textSize">@dimen/text_normal</item>
        <item name="android:fontFamily">@font/avenir_next_regular</item>

        <!-- set primary toolbar & menu style -->
        <item name="toolbarStyle">@style/Passlocker.ToolBar</item>
        <item name="android:actionOverflowButtonStyle">@style/Passlocker.Overflow.Menu</item>

        <!-- set default primary button style -->
        <item name="materialButtonStyle">@style/Passlocker.Primary.Button</item>

        <!-- set default compound button style -->
        <item name="checkboxStyle">@style/Passlocker.CheckBox</item>
        <item name="radioButtonStyle">@style/Passlocker.RadioButton</item>
        <item name="switchStyle">@style/Passlocker.Switch</item>

        <item name="editTextStyle">@style/Passlocker.Edittext</item>
        <item name="cardViewStyle">@style/Passlocker.CardView</item>

        <item name="dialogTheme">@style/Passlocker.Dialog</item>
    </style>
</resources>